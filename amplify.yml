version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd curio-news-ui && npm install
    build:
      commands:
        - cd curio-news-ui && npm run build
  artifacts:
    baseDirectory: curio-news-ui/build
    files:
      - '**/*'