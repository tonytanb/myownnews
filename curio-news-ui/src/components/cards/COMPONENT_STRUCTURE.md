# CurioCardStack Component Structure

## Visual Hierarchy

```
┌─────────────────────────────────────────────────────────────┐
│                    CurioCardStack                           │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              Progress Bar (3px)                       │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐  │
│  │         AnimatePresence (Framer Motion)               │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │         motion.div (Card Wrapper)               │  │  │
│  │  │                                                 │  │  │
│  │  │  ┌───────────────────────────────────────────┐  │  │  │
│  │  │  │   OverviewCard (type === 'overview')     │  │  │  │
│  │  │  │   OR                                     │  │  │  │
│  │  │  │   StoryCard (type === CategoryType)     │  │  │  │
│  │  │  │                                          │  │  │  │
│  │  │  │   ┌─────────────────────────────────┐   │  │  │  │
│  │  │  │   │   BackgroundMedia               │   │  │  │  │
│  │  │  │   │   (video/image/GIF)             │   │  │  │  │
│  │  │  │   └─────────────────────────────────┘   │  │  │  │
│  │  │  │                                          │  │  │  │
│  │  │  │   ┌─────────────────────────────────┐   │  │  │  │
│  │  │  │   │   CategoryTag                   │   │  │  │  │
│  │  │  │   └─────────────────────────────────┘   │  │  │  │
│  │  │  │                                          │  │  │  │
│  │  │  │   ┌─────────────────────────────────┐   │  │  │  │
│  │  │  │   │   Story Content                 │   │  │  │  │
│  │  │  │   │   - Title                       │   │  │  │  │
│  │  │  │   │   - Summary                     │   │  │  │  │
│  │  │  │   └─────────────────────────────────┘   │  │  │  │
│  │  │  │                                          │  │  │  │
│  │  │  │   [🔊] Audio Button   ● ○ ○ ○ ○ Dots   │  │  │  │
│  │  │  └───────────────────────────────────────┘  │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │   Navigation Hint (only on first card)           │  │
│  │   "Swipe or tap to navigate"                     │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

## Component Tree

```
CurioCardStack
├── State Management
│   ├── currentCardIndex: number
│   ├── cards: StoryCard[]
│   ├── isTransitioning: boolean
│   └── preloadedMedia: Map<number, HTMLVideoElement | HTMLImageElement>
│
├── Effects
│   ├── Initialize cards from bootstrapData
│   ├── Preload media for next 2 cards
│   └── Keyboard event listeners
│
├── Navigation Functions
│   ├── nextCard()
│   ├── previousCard()
│   └── handleCardTap()
│
├── Gesture Handlers (react-swipeable)
│   ├── onSwipedLeft → nextCard()
│   └── onSwipedRight → previousCard()
│
└── Render
    ├── Progress Bar
    ├── AnimatePresence
    │   └── motion.div (with cardVariants)
    │       ├── OverviewCard (if type === 'overview')
    │       │   ├── BackgroundMedia
    │       │   ├── Sparkles Icon
    │       │   ├── Title & Date
    │       │   ├── Highlights
    │       │   └── CTA
    │       │
    │       └── StoryCard (if type === CategoryType)
    │           ├── BackgroundMedia
    │           ├── CategoryTag
    │           ├── Watermark
    │           ├── Story Content
    │           ├── Audio Button
    │           └── Navigation Dots
    │
    └── Navigation Hint (conditional)
```

## Data Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    Bootstrap API                            │
│  { news_items, script, word_timings, agentOutputs }        │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│              transformToCards()                             │
│  - Create overview card                                     │
│  - Segment script by word timings                           │
│  - Create favorite story card                               │
│  - Create story cards from news items                       │
│  - Create entertainment cards                               │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│              cards: StoryCard[]                             │
│  [OverviewCard, FavoriteCard, ...StoryCards, ...EntCards]  │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│         CurioCardStack State                                │
│  - currentCardIndex: 0                                      │
│  - cards: StoryCard[]                                       │
│  - isTransitioning: false                                   │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│         Render Current Card                                 │
│  cards[currentCardIndex]                                    │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│         User Interaction                                    │
│  - Tap/Click → nextCard()                                   │
│  - Swipe Left → nextCard()                                  │
│  - Swipe Right → previousCard()                             │
│  - Arrow Keys → next/previousCard()                         │
│  - Audio Button → onAudioPlay(index, timestamp)             │
└─────────────────────────────────────────────────────────────┘
```

## Animation Flow

```
Card Transition Sequence:

1. User triggers navigation (tap/swipe/keyboard)
   ↓
2. Check: isTransitioning? → Exit if true
   ↓
3. Set isTransitioning = true
   ↓
4. Update currentCardIndex
   ↓
5. AnimatePresence detects key change
   ↓
6. Current card: exit animation (300ms)
   - opacity: 1 → 0
   - y: 0 → -50
   - scale: 1 → 0.95
   ↓
7. New card: enter animation (500ms)
   - opacity: 0 → 1
   - y: 50 → 0
   - scale: 0.95 → 1
   ↓
8. After 500ms: Set isTransitioning = false
   ↓
9. Preload next 2 cards' media
```

## State Transitions

```
┌─────────────┐
│   Initial   │
│  index: 0   │
└──────┬──────┘
       │
       │ nextCard()
       ▼
┌─────────────┐
│Transitioning│
│  index: 0   │
│ trans: true │
└──────┬──────┘
       │
       │ 500ms
       ▼
┌─────────────┐
│   Stable    │
│  index: 1   │
│ trans: false│
└──────┬──────┘
       │
       │ previousCard()
       ▼
┌─────────────┐
│Transitioning│
│  index: 1   │
│ trans: true │
└──────┬──────┘
       │
       │ 500ms
       ▼
┌─────────────┐
│   Stable    │
│  index: 0   │
│ trans: false│
└─────────────┘
```

## Media Preloading Strategy

```
Current Card Index: 3

┌─────┬─────┬─────┬─────┬─────┬─────┬─────┐
│  0  │  1  │  2  │  3  │  4  │  5  │  6  │
│     │     │     │ 👁️  │ ⏳  │ ⏳  │     │
└─────┴─────┴─────┴─────┴─────┴─────┴─────┘
                    ▲     ▲     ▲
                    │     │     │
                Current  +1    +2
                         Preload

Legend:
👁️ = Currently viewing
⏳ = Preloaded
```

## Responsive Breakpoints

```
Mobile (< 768px):
┌─────────────────────────────────┐
│                                 │
│         Full Viewport           │
│         100vw × 100vh           │
│                                 │
│         Card Content            │
│                                 │
└─────────────────────────────────┘

Desktop (≥ 768px):
┌─────────────────────────────────────────────────┐
│              Black Background                   │
│                                                 │
│         ┌─────────────────────┐                │
│         │   Centered Card     │                │
│         │   380px × 680px     │                │
│         │   Border Radius     │                │
│         │   Box Shadow        │                │
│         └─────────────────────┘                │
│                                                 │
└─────────────────────────────────────────────────┘
```

## Event Flow

```
User Action → Event Handler → State Update → Re-render → Animation

Examples:

1. Tap Card:
   onClick → handleCardTap() → nextCard() → setCurrentCardIndex(i+1) → AnimatePresence

2. Swipe Left:
   onSwipedLeft → nextCard() → setCurrentCardIndex(i+1) → AnimatePresence

3. Press Arrow Right:
   onKeyDown → nextCard() → setCurrentCardIndex(i+1) → AnimatePresence

4. Click Audio Button:
   onClick → handleAudioPlay() → onAudioPlay(index, timestamp) → Parent handles
```

## Integration Points

```
┌─────────────────────────────────────────────────────────────┐
│                        App.tsx                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              CurioCardStack                           │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │         OverviewCard / StoryCard              │  │  │
│  │  │  ┌───────────────────────────────────────────┐  │  │  │
│  │  │  │      BackgroundMedia                     │  │  │  │
│  │  │  └───────────────────────────────────────────┘  │  │  │
│  │  │  ┌───────────────────────────────────────────┐  │  │  │
│  │  │  │      CategoryTag                         │  │  │  │
│  │  │  └───────────────────────────────────────────┘  │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              AudioPlayer (Global)                     │  │
│  │  - Receives onAudioPlay callbacks                     │  │
│  │  - Manages audio element                              │  │
│  │  - Syncs with card timestamps                         │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              AgentTrace (Optional)                    │  │
│  │  - Shows agent collaboration                          │  │
│  │  - Analytics overlay                                  │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## File Dependencies

```
CurioCardStack.tsx
├── React (useState, useEffect, useCallback)
├── framer-motion (motion, AnimatePresence)
├── react-swipeable (useSwipeable)
├── ./types (StoryCard, BootstrapResponse)
├── ../../utils/cardTransformer (transformToCards)
├── ./OverviewCard
├── ./StoryCard
└── ./CurioCardStack.css

CurioCardStack.css
├── Mobile-first styles
├── Desktop responsive styles
├── Animation keyframes
├── Accessibility features
└── Performance optimizations
```
